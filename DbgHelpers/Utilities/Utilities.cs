using System.Diagnostics;

namespace DbgHelpers.Utilities
{
    public class Utilities
    {
        //public static string DebugMode2Description( DebuggableAttribute.DebuggingModes mode )
        //{
        //    string desc = "Tracking On";

        //    if ( (int)mode % 2 == 1)
        //        desc += ", Default";

        //    if ( ((int)mode / (int)DebuggableAttribute.DebuggingModes.DisableOptimizations) == 1 )
        //        desc += ", "
        //}
    }
}
